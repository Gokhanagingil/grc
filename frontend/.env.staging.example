# =============================================================================
# GRC Platform - Frontend Staging Environment Configuration
# =============================================================================
# This file is used as a reference for staging environment setup.
# The actual API URL is set at Docker build time via build args.
#
# For staging deployment (e.g., https://niles-grc.com):
# =============================================================================

# Backend API URL for staging
# RECOMMENDED: Use relative /api path for single-origin architecture
# This routes all API requests through the NGINX reverse proxy, eliminating
# CORS issues and corporate firewall problems.
REACT_APP_API_URL=/api

# LEGACY (not recommended): Direct backend access
# Only use this for debugging or if reverse proxy is not available
# REACT_APP_API_URL=https://api.niles-grc.com

# =============================================================================
# Build Command Example (API Gateway Architecture):
# =============================================================================
# docker build \
#   --build-arg REACT_APP_API_URL=/api \
#   -t grc-frontend:staging \
#   .
#
# Or use docker-compose which sets this automatically:
#   docker compose -f docker-compose.staging.yml up -d --build
# =============================================================================
