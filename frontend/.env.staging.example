# =============================================================================
# GRC Platform - Frontend Staging Environment Configuration
# =============================================================================
# This file is used as a reference for staging environment setup.
# The actual API URL is set at Docker build time via build args.
#
# For staging deployment on Hetzner (46.224.99.150):
# =============================================================================

# Backend API URL for staging
# This should point to the backend service
# Option 1: Direct IP access (if backend port is exposed)
REACT_APP_API_URL=http://46.224.99.150:3002

# Option 2: If using a reverse proxy or same-origin setup
# REACT_APP_API_URL=/api

# =============================================================================
# Build Command Example:
# =============================================================================
# docker build \
#   --build-arg REACT_APP_API_URL=http://46.224.99.150:3002 \
#   -t grc-frontend:staging \
#   .
# =============================================================================
