# =============================================================================
# GRC Platform - Staging Environment Docker Compose
# =============================================================================
# Staging deployment configuration for Hetzner server
#
# Usage:
#   Start:    docker compose -f docker-compose.staging.yml up -d
#   Stop:     docker compose -f docker-compose.staging.yml down
#   Rebuild:  docker compose -f docker-compose.staging.yml up -d --build --force-recreate
#   Logs:     docker compose -f docker-compose.staging.yml logs -f
#
# After starting, access:
#   - Frontend: http://46.224.99.150
#   - Backend API: http://46.224.99.150:3002 (direct) or http://46.224.99.150/* (via nginx proxy)
#   - Backend Health: http://46.224.99.150/health (proxied to backend)
#   - Frontend Health: http://46.224.99.150/frontend-health
#
# =============================================================================

services:
  # ---------------------------------------------------------------------------
  # PostgreSQL Database
  # ---------------------------------------------------------------------------
  db:
    image: postgres:15-alpine
    container_name: grc-staging-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-grc_platform}
    volumes:
      - grc_staging_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-grc_platform}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - grc-staging-network

  # ---------------------------------------------------------------------------
  # NestJS Backend API
  # ---------------------------------------------------------------------------
  backend:
    build:
      context: ./backend-nest
      dockerfile: Dockerfile
    container_name: grc-staging-backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Application
      NODE_ENV: production
      PORT: 3002
      
      # Database
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_NAME: ${DB_NAME:-grc_platform}
      DB_SYNC: ${DB_SYNC:-false}
      
      # Security
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET environment variable is required}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-24h}
      
      # CORS - Allow staging domain
      CORS_ORIGINS: ${CORS_ORIGINS:-http://46.224.99.150,http://localhost:3000,http://localhost:3001,http://localhost:3002}
      
      # Demo admin (staging)
      DEMO_ADMIN_EMAIL: ${DEMO_ADMIN_EMAIL}
      DEMO_ADMIN_PASSWORD: ${DEMO_ADMIN_PASSWORD}
    ports:
      - "3002:3002"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3002/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - grc-staging-network

  # ---------------------------------------------------------------------------
  # React Frontend (Nginx)
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Empty string means use relative URL (nginx reverse proxy handles routing)
        # This allows API calls to go to same origin, which nginx proxies to backend
        REACT_APP_API_URL: ""
    container_name: grc-staging-frontend
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/frontend-health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - grc-staging-network

# -----------------------------------------------------------------------------
# Named Volumes
# -----------------------------------------------------------------------------
volumes:
  grc_staging_postgres_data:
    driver: local

# -----------------------------------------------------------------------------
# Networks
# -----------------------------------------------------------------------------
networks:
  grc-staging-network:
    driver: bridge

