name: Backend CI

on:
  push:
    branches:
      - main
      - 'devin/**'
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./backend
    
    strategy:
      matrix:
        node-version: [20.x]
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run tests
        run: npm run test:ci
        env:
          NODE_ENV: test
          DB_CLIENT: sqlite
          DB_PATH: ':memory:'
          JWT_SECRET: 'ci-test-jwt-secret-for-testing-purposes-only-32chars'
          JWT_EXPIRES_IN: '1h'
          AUTH_RATE_LIMIT_MAX: '1000'
          RATE_LIMIT_MAX: '1000'
          CORS_ORIGINS: 'http://localhost:3000,http://localhost:3001'
      
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: backend/coverage/
          retention-days: 7

  # Future: Add PostgreSQL integration tests
  # integration-test:
  #   name: Integration Tests (PostgreSQL)
  #   runs-on: ubuntu-latest
  #   needs: lint-and-test
  #   
  #   services:
  #     postgres:
  #       image: postgres:15
  #       env:
  #         POSTGRES_USER: postgres
  #         POSTGRES_PASSWORD: postgres
  #         POSTGRES_DB: grc_platform_test
  #       ports:
  #         - 5432:5432
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #   
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20.x
  #     - run: npm ci
  #       working-directory: ./backend
  #     - run: npm run db:init:pg
  #       working-directory: ./backend
  #       env:
  #         DB_CLIENT: pg
  #         DB_HOST: localhost
  #         DB_PORT: 5432
  #         DB_USER: postgres
  #         DB_PASSWORD: postgres
  #         DB_NAME: grc_platform_test
  #     - run: npm run test:ci
  #       working-directory: ./backend
  #       env:
  #         DB_CLIENT: pg
  #         DB_HOST: localhost
  #         DB_PORT: 5432
  #         DB_USER: postgres
  #         DB_PASSWORD: postgres
  #         DB_NAME: grc_platform_test
