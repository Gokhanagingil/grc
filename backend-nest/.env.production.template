# =============================================================================
# GRC Platform - Production Environment Configuration Template
# =============================================================================
# IMPORTANT: This is a TEMPLATE file. Copy to .env.production and fill in
# all values with production-appropriate settings.
#
# DO NOT commit actual production secrets to version control!
# =============================================================================

NODE_ENV=production
PORT=3002

# Database - Production PostgreSQL
# Use a managed PostgreSQL service (AWS RDS, Azure Database, etc.)
DB_HOST=your-production-db-host.example.com
DB_PORT=5432
DB_USER=grc_app_user
DB_PASSWORD=<STRONG_PASSWORD_HERE>
DB_NAME=grc_platform

# CRITICAL: Never enable DB_SYNC in production!
# Use database migrations instead.
DB_SYNC=false

# Security - Production JWT secret
# Generate with: openssl rand -base64 64
JWT_SECRET=<GENERATE_STRONG_SECRET_64_CHARS_MINIMUM>
JWT_EXPIRES_IN=8h

# CORS - Production allowed origins
# List only your actual frontend domains
CORS_ORIGINS=https://your-frontend-domain.com

# Demo credentials - REMOVE or change in production!
# These should be disabled or use strong passwords
DEMO_ADMIN_EMAIL=admin@your-domain.com
DEMO_ADMIN_PASSWORD=<STRONG_PASSWORD_HERE>

# Audit logging - Keep enabled for compliance
NEST_AUDIT_LOG_ENABLED=true

# =============================================================================
# Production Checklist:
# =============================================================================
# [ ] All secrets are strong and unique
# [ ] DB_SYNC is set to false
# [ ] CORS_ORIGINS only includes production domains
# [ ] Demo credentials are changed or disabled
# [ ] SSL/TLS is configured for database connections
# [ ] Application is behind a reverse proxy (nginx, etc.)
# [ ] Health checks are configured in load balancer
# [ ] Logging is configured to aggregate to central system
# [ ] Monitoring and alerting are set up
# =============================================================================
