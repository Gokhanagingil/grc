<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Audit Report - {{audit.name}}</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #333;
      background: #fff;
      padding: 40px;
      max-width: 1200px;
      margin: 0 auto;
    }
    .report-header {
      border-bottom: 3px solid #1976d2;
      padding-bottom: 20px;
      margin-bottom: 30px;
    }
    .report-title {
      font-size: 28px;
      color: #1976d2;
      margin-bottom: 10px;
    }
    .report-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      font-size: 14px;
      color: #666;
    }
    .report-meta-item {
      display: flex;
      gap: 5px;
    }
    .report-meta-label {
      font-weight: 600;
    }
    .section {
      margin-bottom: 40px;
    }
    .section-title {
      font-size: 20px;
      color: #1976d2;
      border-bottom: 2px solid #e0e0e0;
      padding-bottom: 10px;
      margin-bottom: 20px;
    }
    .subsection-title {
      font-size: 16px;
      color: #424242;
      margin: 20px 0 10px;
      font-weight: 600;
    }
    .executive-summary {
      background: #f5f5f5;
      padding: 20px;
      border-radius: 8px;
      margin-bottom: 20px;
    }
    .metrics-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }
    .metric-card {
      background: #fff;
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      padding: 20px;
      text-align: center;
    }
    .metric-value {
      font-size: 36px;
      font-weight: 700;
      color: #1976d2;
    }
    .metric-label {
      font-size: 14px;
      color: #666;
      margin-top: 5px;
    }
    .severity-heatmap {
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
      margin-bottom: 20px;
    }
    .severity-item {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 10px 15px;
      border-radius: 6px;
      min-width: 120px;
    }
    .severity-critical {
      background: #ffebee;
      border: 1px solid #f44336;
    }
    .severity-high {
      background: #fff3e0;
      border: 1px solid #ff9800;
    }
    .severity-medium {
      background: #e3f2fd;
      border: 1px solid #2196f3;
    }
    .severity-low {
      background: #e8f5e9;
      border: 1px solid #4caf50;
    }
    .severity-count {
      font-size: 24px;
      font-weight: 700;
    }
    .severity-label {
      font-size: 12px;
      text-transform: uppercase;
    }
    .badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
    }
    .badge-critical { background: #f44336; color: #fff; }
    .badge-high { background: #ff9800; color: #fff; }
    .badge-medium { background: #2196f3; color: #fff; }
    .badge-low { background: #4caf50; color: #fff; }
    .badge-draft { background: #9e9e9e; color: #fff; }
    .badge-in-progress { background: #2196f3; color: #fff; }
    .badge-closed { background: #4caf50; color: #fff; }
    .badge-overdue { background: #f44336; color: #fff; }
    .badge-validated { background: #4caf50; color: #fff; }
    .badge-not-validated { background: #ff9800; color: #fff; }
    .badge-rejected { background: #f44336; color: #fff; }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    th, td {
      padding: 12px;
      text-align: left;
      border-bottom: 1px solid #e0e0e0;
    }
    th {
      background: #f5f5f5;
      font-weight: 600;
      color: #424242;
    }
    tr:hover {
      background: #fafafa;
    }
    .finding-card {
      background: #fff;
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      margin-bottom: 20px;
      overflow: hidden;
    }
    .finding-header {
      padding: 15px 20px;
      background: #f5f5f5;
      border-bottom: 1px solid #e0e0e0;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .finding-title {
      font-size: 16px;
      font-weight: 600;
      color: #333;
    }
    .finding-body {
      padding: 20px;
    }
    .finding-section {
      margin-bottom: 15px;
    }
    .finding-section-label {
      font-size: 12px;
      color: #666;
      text-transform: uppercase;
      margin-bottom: 5px;
    }
    .finding-section-content {
      color: #333;
    }
    .capa-list {
      margin-top: 15px;
      padding-top: 15px;
      border-top: 1px solid #e0e0e0;
    }
    .capa-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px;
      background: #fafafa;
      border-radius: 4px;
      margin-bottom: 8px;
    }
    .capa-title {
      font-weight: 500;
    }
    .capa-meta {
      display: flex;
      gap: 10px;
      font-size: 12px;
      color: #666;
    }
    .timeline {
      position: relative;
      padding-left: 30px;
    }
    .timeline::before {
      content: '';
      position: absolute;
      left: 10px;
      top: 0;
      bottom: 0;
      width: 2px;
      background: #e0e0e0;
    }
    .timeline-item {
      position: relative;
      margin-bottom: 20px;
    }
    .timeline-item::before {
      content: '';
      position: absolute;
      left: -24px;
      top: 5px;
      width: 10px;
      height: 10px;
      border-radius: 50%;
      background: #1976d2;
    }
    .empty-state {
      text-align: center;
      padding: 40px;
      color: #666;
      font-style: italic;
    }
    .risk-level-indicator {
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }
    .risk-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
    }
    .risk-dot-critical { background: #f44336; }
    .risk-dot-high { background: #ff9800; }
    .risk-dot-medium { background: #2196f3; }
    .risk-dot-low { background: #4caf50; }
    .footer {
      margin-top: 60px;
      padding-top: 20px;
      border-top: 2px solid #e0e0e0;
      text-align: center;
      font-size: 12px;
      color: #666;
    }
    @media print {
      body {
        padding: 20px;
      }
      .finding-card {
        break-inside: avoid;
      }
    }
  </style>
</head>
<body>
  <div class="report-header">
    <h1 class="report-title">{{audit.name}}</h1>
    <div class="report-meta">
      <div class="report-meta-item">
        <span class="report-meta-label">Report Version:</span>
        <span>{{reportVersion}}</span>
      </div>
      <div class="report-meta-item">
        <span class="report-meta-label">Status:</span>
        <span>{{reportStatus}}</span>
      </div>
      <div class="report-meta-item">
        <span class="report-meta-label">Audit Type:</span>
        <span>{{audit.audit_type}}</span>
      </div>
      <div class="report-meta-item">
        <span class="report-meta-label">Risk Level:</span>
        <span class="risk-level-indicator">
          <span class="risk-dot risk-dot-{{audit.risk_level}}"></span>
          {{audit.risk_level}}
        </span>
      </div>
      <div class="report-meta-item">
        <span class="report-meta-label">Department:</span>
        <span>{{#if audit.department}}{{audit.department}}{{else}}N/A{{/if}}</span>
      </div>
      <div class="report-meta-item">
        <span class="report-meta-label">Generated:</span>
        <span>{{generatedAt}}</span>
      </div>
    </div>
  </div>

  <div class="section">
    <h2 class="section-title">Executive Summary</h2>
    <div class="executive-summary">
      <p>{{#if audit.description}}{{audit.description}}{{else}}No description provided.{{/if}}</p>
      {{#if audit.findings_summary}}
      <h3 class="subsection-title">Findings Summary</h3>
      <p>{{audit.findings_summary}}</p>
      {{/if}}
      {{#if audit.conclusion}}
      <h3 class="subsection-title">Conclusion</h3>
      <p>{{audit.conclusion}}</p>
      {{/if}}
    </div>
    
    <div class="metrics-grid">
      <div class="metric-card">
        <div class="metric-value">{{metrics.totalFindings}}</div>
        <div class="metric-label">Total Findings</div>
      </div>
      <div class="metric-card">
        <div class="metric-value">{{metrics.totalCapas}}</div>
        <div class="metric-label">Total CAPAs</div>
      </div>
      <div class="metric-card">
        <div class="metric-value">{{metrics.openCapas}}</div>
        <div class="metric-label">Open CAPAs</div>
      </div>
      <div class="metric-card">
        <div class="metric-value">{{metrics.overdueCapas}}</div>
        <div class="metric-label">Overdue CAPAs</div>
      </div>
    </div>
  </div>

  <div class="section">
    <h2 class="section-title">Severity Heatmap</h2>
    <div class="severity-heatmap">
      <div class="severity-item severity-critical">
        <span class="severity-count">{{metrics.findingsBySeverity.critical}}</span>
        <span class="severity-label">Critical</span>
      </div>
      <div class="severity-item severity-high">
        <span class="severity-count">{{metrics.findingsBySeverity.high}}</span>
        <span class="severity-label">High</span>
      </div>
      <div class="severity-item severity-medium">
        <span class="severity-count">{{metrics.findingsBySeverity.medium}}</span>
        <span class="severity-label">Medium</span>
      </div>
      <div class="severity-item severity-low">
        <span class="severity-count">{{metrics.findingsBySeverity.low}}</span>
        <span class="severity-label">Low</span>
      </div>
    </div>
  </div>

  <div class="section">
    <h2 class="section-title">Audit Schedule</h2>
    <table>
      <tr>
        <th>Planned Start</th>
        <th>Planned End</th>
        <th>Actual Start</th>
        <th>Actual End</th>
      </tr>
      <tr>
        <td>{{#if audit.planned_start_date}}{{audit.planned_start_date}}{{else}}N/A{{/if}}</td>
        <td>{{#if audit.planned_end_date}}{{audit.planned_end_date}}{{else}}N/A{{/if}}</td>
        <td>{{#if audit.actual_start_date}}{{audit.actual_start_date}}{{else}}N/A{{/if}}</td>
        <td>{{#if audit.actual_end_date}}{{audit.actual_end_date}}{{else}}N/A{{/if}}</td>
      </tr>
    </table>
  </div>

  {{#if audit.scope}}
  <div class="section">
    <h2 class="section-title">Scope</h2>
    <p>{{audit.scope}}</p>
  </div>
  {{/if}}

  {{#if audit.objectives}}
  <div class="section">
    <h2 class="section-title">Objectives</h2>
    <p>{{audit.objectives}}</p>
  </div>
  {{/if}}

  {{#if audit.methodology}}
  <div class="section">
    <h2 class="section-title">Methodology</h2>
    <p>{{audit.methodology}}</p>
  </div>
  {{/if}}

  <div class="section">
    <h2 class="section-title">Criteria Coverage</h2>
    {{#if criteria.length}}
    <table>
      <thead>
        <tr>
          <th>Requirement</th>
          <th>Regulation</th>
          <th>Category</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
        {{#each criteria}}
        <tr>
          <td>{{this.title}}</td>
          <td>{{#if this.regulation}}{{this.regulation}}{{else}}N/A{{/if}}</td>
          <td>{{#if this.category}}{{this.category}}{{else}}N/A{{/if}}</td>
          <td>{{#if this.status}}<span class="badge badge-{{this.status}}">{{this.status}}</span>{{else}}N/A{{/if}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    {{else}}
    <div class="empty-state">No criteria linked to this audit.</div>
    {{/if}}
  </div>

  <div class="section">
    <h2 class="section-title">Scope Objects</h2>
    {{#if scopeObjects.length}}
    <table>
      <thead>
        <tr>
          <th>Object Type</th>
          <th>Object ID</th>
          <th>Name</th>
        </tr>
      </thead>
      <tbody>
        {{#each scopeObjects}}
        <tr>
          <td><span class="badge">{{this.object_type}}</span></td>
          <td>{{this.object_id}}</td>
          <td>{{#if this.object_name}}{{this.object_name}}{{else}}N/A{{/if}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    {{else}}
    <div class="empty-state">No scope objects defined for this audit.</div>
    {{/if}}
  </div>

  <div class="section">
    <h2 class="section-title">Detailed Findings</h2>
    {{#if findings.length}}
    {{#each findings}}
    <div class="finding-card">
      <div class="finding-header">
        <span class="finding-title">{{this.title}}</span>
        <div>
          <span class="badge badge-{{this.severity}}">{{this.severity}}</span>
          <span class="badge badge-{{this.status}}">{{this.status}}</span>
        </div>
      </div>
      <div class="finding-body">
        {{#if this.description}}
        <div class="finding-section">
          <div class="finding-section-label">Description</div>
          <div class="finding-section-content">{{this.description}}</div>
        </div>
        {{/if}}
        
        {{#if this.root_cause}}
        <div class="finding-section">
          <div class="finding-section-label">Root Cause</div>
          <div class="finding-section-content">{{this.root_cause}}</div>
        </div>
        {{/if}}
        
        {{#if this.recommendation}}
        <div class="finding-section">
          <div class="finding-section-label">Recommendation</div>
          <div class="finding-section-content">{{this.recommendation}}</div>
        </div>
        {{/if}}
        
        {{#if this.management_response}}
        <div class="finding-section">
          <div class="finding-section-label">Management Response</div>
          <div class="finding-section-content">{{this.management_response}}</div>
        </div>
        {{/if}}

        {{#if this.breached_requirements.length}}
        <div class="finding-section">
          <div class="finding-section-label">Breached Requirements</div>
          <div class="finding-section-content">
            {{#each this.breached_requirements}}
            <span class="badge">{{this.requirement_title}} ({{this.regulation}})</span>
            {{/each}}
          </div>
        </div>
        {{/if}}

        {{#if this.related_risks.length}}
        <div class="finding-section">
          <div class="finding-section-label">Related Risks</div>
          <div class="finding-section-content">
            {{#each this.related_risks}}
            <span class="risk-level-indicator">
              <span class="risk-dot risk-dot-{{this.risk_severity}}"></span>
              {{this.risk_title}}
            </span>
            {{/each}}
          </div>
        </div>
        {{/if}}

        {{#if this.itsm_links.length}}
        <div class="finding-section">
          <div class="finding-section-label">ITSM Links</div>
          <div class="finding-section-content">
            {{#each this.itsm_links}}
            <span class="badge">{{this.itsm_type}}: {{this.itsm_id}}</span>
            {{/each}}
          </div>
        </div>
        {{/if}}
        
        {{#if this.capas.length}}
        <div class="capa-list">
          <div class="finding-section-label">Corrective/Preventive Actions (CAPAs)</div>
          {{#each this.capas}}
          <div class="capa-item">
            <div>
              <span class="capa-title">{{this.title}}</span>
              <span class="badge badge-{{this.type}}">{{this.type}}</span>
            </div>
            <div class="capa-meta">
              <span>Due: {{#if this.due_date}}{{this.due_date}}{{else}}N/A{{/if}}</span>
              <span class="badge badge-{{this.status}}">{{this.status}}</span>
              <span class="badge badge-{{this.validation_status}}">{{this.validation_status}}</span>
            </div>
          </div>
          {{/each}}
        </div>
        {{/if}}
      </div>
    </div>
    {{/each}}
    {{else}}
    <div class="empty-state">No findings recorded for this audit.</div>
    {{/if}}
  </div>

  <div class="section">
    <h2 class="section-title">Evidence</h2>
    {{#if evidence.length}}
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Type</th>
          <th>Storage Type</th>
          <th>Reference</th>
          <th>Uploaded By</th>
          <th>Uploaded At</th>
        </tr>
      </thead>
      <tbody>
        {{#each evidence}}
        <tr>
          <td>{{this.title}}</td>
          <td><span class="badge">{{this.type}}</span></td>
          <td>{{this.storage_type}}</td>
          <td>{{#if this.storage_ref}}{{this.storage_ref}}{{else if this.external_system}}{{this.external_system}}: {{this.external_id}}{{else}}N/A{{/if}}</td>
          <td>{{#if this.uploaded_by_first_name}}{{this.uploaded_by_first_name}} {{this.uploaded_by_last_name}}{{else}}N/A{{/if}}</td>
          <td>{{#if this.uploaded_at}}{{this.uploaded_at}}{{else}}N/A{{/if}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
    {{else}}
    <div class="empty-state">No evidence attached to this audit.</div>
    {{/if}}
  </div>

  {{#if audit.recommendations}}
  <div class="section">
    <h2 class="section-title">Recommendations</h2>
    <p>{{audit.recommendations}}</p>
  </div>
  {{/if}}

  <div class="section">
    <h2 class="section-title">Audit Team</h2>
    <table>
      <tr>
        <th>Role</th>
        <th>Name</th>
        <th>Email</th>
      </tr>
      <tr>
        <td>Owner</td>
        <td>{{#if audit.owner_first_name}}{{audit.owner_first_name}} {{audit.owner_last_name}}{{else}}Not Assigned{{/if}}</td>
        <td>{{#if audit.owner_email}}{{audit.owner_email}}{{else}}N/A{{/if}}</td>
      </tr>
      <tr>
        <td>Lead Auditor</td>
        <td>{{#if audit.lead_auditor_first_name}}{{audit.lead_auditor_first_name}} {{audit.lead_auditor_last_name}}{{else}}Not Assigned{{/if}}</td>
        <td>{{#if audit.lead_auditor_email}}{{audit.lead_auditor_email}}{{else}}N/A{{/if}}</td>
      </tr>
    </table>
  </div>

  <div class="footer">
    <p>This report was generated on {{generatedAt}} by the GRC Platform Audit Reporting Engine.</p>
    <p>Report Version: {{reportVersion}} | Status: {{reportStatus}}</p>
    <p>Generated by: {{#if generatedBy.first_name}}{{generatedBy.first_name}} {{generatedBy.last_name}}{{else}}System{{/if}}</p>
  </div>
</body>
</html>
